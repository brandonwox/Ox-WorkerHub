# Dev Tracker
This file is used by the developer of Ox WorkerHub. (Agents may use this file in accordance to instructions from the developer.)


# Awaiting

scheduler: the work requests backlog: the "Schedule" button's background color should be the target crews color. (whatever crew is currently the assigned target.)

work request creation form:
1. clicking the "add scopes" button makes you click another button "Search scopes..." before it shows the dropdown of scopes. Show the dropdown after clicking the first button "add scopes". Also, if the dropdown is open, I shouldn't have to click "Done" to dismiss it, I should be able to click anywhere outside of the dropdown to dismiss it.

On job creation: 
1. when the user selects a scope a new option should appear to fill out how many total of that scope there are for the job. So if the user selects the window scope while creating a job, a new section should appear that requires the user to add an amount of windows for the job. (0 is an acceptable amount, just for weird scenarios.) and the window scope also holds the sgd count too. (the window scope should be the only scope with two counts.)
2. also in job creation form should be an optional builder input. It's already an option if the user wants to edit an already existing job, but there should be an optional field to add the builder on job creation as well.


# Unsure



# DONE

Completed edits live in [Dev-Tracker-Done.md](Dev-Tracker-Done.md), newest first. When an Awaiting edit above is implemented, remove it from this file and log it at the top of that one.
